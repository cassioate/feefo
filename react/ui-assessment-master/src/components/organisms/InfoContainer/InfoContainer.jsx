import React from "react";
import { StyledInfoContainer } from "./InfoContainer.js";

const InfoContainer = ({ children }) => {
  return <StyledInfoContainer>{children}</StyledInfoContainer>;
};

export default InfoContainer;
