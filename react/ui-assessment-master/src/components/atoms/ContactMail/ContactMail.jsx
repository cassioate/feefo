import React from "react";
import { StyledContactMail } from "./ContactMail.js";

const ContactMail = ({ children }) => {
  return <StyledContactMail>{children}</StyledContactMail>;
};

export default ContactMail;
