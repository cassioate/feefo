import React from "react";
import { StyledLetterIcon } from "./LetterIcon.js";

const LetterIcon = ({ children }) => {
  return <StyledLetterIcon>{children}</StyledLetterIcon>;
};

export default LetterIcon;
