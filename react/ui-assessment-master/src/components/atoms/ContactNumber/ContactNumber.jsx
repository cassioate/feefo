import React from "react";
import { StyledContactNumber } from "./ContactNumber.js";

const ContactNumber = ({ children }) => {
  return <StyledContactNumber>{children}</StyledContactNumber>;
};

export default ContactNumber;
