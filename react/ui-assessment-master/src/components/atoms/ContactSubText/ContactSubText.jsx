import React from "react";
import { StyledContactSubText } from "./ContactSubText.js";

const ContactSubText = ({ children }) => {
  return <StyledContactSubText>{children}</StyledContactSubText>;
};

export default ContactSubText;
